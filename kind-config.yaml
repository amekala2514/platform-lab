kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: platform-lab

nodes:
  - role: control-plane
    # Expose port 80 and 443 on the host for future ingress use
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8090
        protocol: TCP
  - role: worker
  - role: worker
