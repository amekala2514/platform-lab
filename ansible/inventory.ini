[local]
localhost ansible_connection=local

[local:vars]
# Homebrew prefix differs by chip: Apple Silicon uses /opt/homebrew, Intel uses /usr/local
homebrew_prefix=/opt/homebrew
project_root={{ ansible_env.HOME }}/platform-lab
